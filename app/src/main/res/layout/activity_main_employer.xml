<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_employer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Contenido Principal -->
    <include layout="@layout/main_employer_content" />

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_employer_view"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="@drawable/menu_light_fondo"
        app:headerLayout="@layout/nav_employer_header"
        app:menu="@menu/menu_main_employer"
        app:itemTextColor="@drawable/drawer_item"
        app:itemIconTint="@drawable/drawer_item"
        app:itemBackground="@color/transparentSquareColor"/>
</android.support.v4.widget.DrawerLayout>
